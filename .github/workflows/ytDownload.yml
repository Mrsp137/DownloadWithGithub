name: YouTube Downloader

on:
  workflow_dispatch:
    inputs:
      video_url:
        description: 'لینک ویدیو یوتیوب'
        required: true
        type: string
      format:
        description: 'فرمت خروجی'
        required: true
        type: choice
        options:
          - mp4
          - mp3
        default: 'mp4'
      quality:
        description: 'کیفیت (برای mp4: 480p/720p/1080p | برای mp3: 128/192/320)'
        required: true
        type: string
        default: '720p'
      user_id:
        description: 'شناسه کاربر'
        required: true
        type: string

permissions:
  contents: write
  actions: read

env:
  TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
  TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}

jobs:

  # ────────────────────────────────────────────
  # JOB 1 : صف — حداکثر ۲ workflow همزمان
  # ────────────────────────────────────────────
  wait-for-queue:
    runs-on: ubuntu-latest
    steps:
      - name: بررسی صف و انتظار
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          REPO="${{ github.repository }}"
          MY_RUN_ID="${{ github.run_id }}"
          MY_RUN_NUMBER="${{ github.run_number }}"
          WORKFLOW_FILE="ytDownload.yml"
          MAX_CONCURRENT=2

          echo "🔍 Run ID: $MY_RUN_ID | Run Number: $MY_RUN_NUMBER"

          POSITION=0
          while true; do
            ACTIVE=$(curl -s \
              -H "Authorization: Bearer $GH_TOKEN" \
              -H "Accept: application/vnd.github+json" \
              "https://api.github.com/repos/${REPO}/actions/runs?workflow_id=${WORKFLOW_FILE}&status=in_progress&per_page=100" \
              | jq "[.workflow_runs[] | select(.id != ($MY_RUN_ID | tonumber) and .run_number < ($MY_RUN_NUMBER | tonumber))] | length")

            if [ "$ACTIVE" -lt "$MAX_CONCURRENT" ]; then
              echo "✅ جای خالی در صف وجود دارد. شروع پردازش..."
              break
            fi

            if [ "$POSITION" -eq 0 ]; then
              curl -s -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage" \
                -d "chat_id=${{ github.event.inputs.user_id }}" \
                --data-urlencode "text=⏳ درخواست شما در صف قرار گرفت. لطفاً صبر کنید..."
              POSITION=1
            fi

            echo "⏳ تعداد runهای فعال: $ACTIVE — انتظار ۳۰ ثانیه..."
            sleep 30
          done

  # ────────────────────────────────────────────
  # JOB 2 : پردازش اصلی
  # ────────────────────────────────────────────
  process:
    needs: wait-for-queue
    runs-on: ubuntu-latest
    steps:

      - name: اطلاع‌رسانی شروع کار
        run: |
          curl -s -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage" \
            -d "chat_id=${TELEGRAM_CHAT_ID}" \
            --data-urlencode "text=✅ شروع دانلود یوتیوب برای کاربر: ${{ github.event.inputs.user_id }}"

          curl -s -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage" \
            -d "chat_id=${{ github.event.inputs.user_id }}" \
            --data-urlencode "text=🚀 دانلود شروع شد، لطفاً منتظر بمانید..."

      - name: Checkout مخزن
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: نصب ابزارها
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y p7zip-full ffmpeg
          pip install yt-dlp --break-system-packages

      - name: دانلود ویدیو با yt-dlp
        id: download
        run: |
          FORMAT="${{ github.event.inputs.format }}"
          QUALITY="${{ github.event.inputs.quality }}"
          URL="${{ github.event.inputs.video_url }}"
          MAX_SIZE=$((200 * 1024 * 1024))  # ۲۰۰ مگابایت
          TIMEOUT=1800                       # ۳۰ دقیقه
          DOWNLOAD_SUCCESS=false

          for attempt in 1 2; do
            echo "📥 تلاش دانلود شماره $attempt ..."

            if [ "$FORMAT" = "mp3" ]; then
              # تنظیم کیفیت صدا
              case "$QUALITY" in
                128) AUDIO_QUALITY="128K" ;;
                192) AUDIO_QUALITY="192K" ;;
                320) AUDIO_QUALITY="320K" ;;
                *)   AUDIO_QUALITY="192K" ;;
              esac

              timeout $TIMEOUT yt-dlp \
                -x \
                --audio-format mp3 \
                --audio-quality "$AUDIO_QUALITY" \
                --no-playlist \
                -o "%(title)s.%(ext)s" \
                "$URL"

            else
              # تنظیم کیفیت ویدیو
              case "$QUALITY" in
                480p)  FORMAT_CODE="bestvideo[height<=480]+bestaudio/best[height<=480]" ;;
                720p)  FORMAT_CODE="bestvideo[height<=720]+bestaudio/best[height<=720]" ;;
                1080p) FORMAT_CODE="bestvideo[height<=1080]+bestaudio/best[height<=1080]" ;;
                *)     FORMAT_CODE="bestvideo[height<=720]+bestaudio/best[height<=720]" ;;
              esac

              timeout $TIMEOUT yt-dlp \
                -f "$FORMAT_CODE" \
                --merge-output-format mp4 \
                --no-playlist \
                -o "%(title)s.%(ext)s" \
                "$URL"
            fi

            EXIT_CODE=$?

            if [ $EXIT_CODE -eq 124 ]; then
              if [ $attempt -eq 1 ]; then
                curl -s -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage" \
                  -d "chat_id=${{ github.event.inputs.user_id }}" \
                  --data-urlencode "text=⚠️ دانلود بیش از ۳۰ دقیقه طول کشید. در حال تلاش مجدد..."
                sleep 5
                continue
              else
                curl -s -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage" \
                  -d "chat_id=${{ github.event.inputs.user_id }}" \
                  --data-urlencode "text=❌ دانلود پس از ۲ تلاش ناموفق بود. لطفاً با ادمین تماس بگیرید: @MeliDownload_Admin"
                curl -s -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage" \
                  -d "chat_id=${TELEGRAM_CHAT_ID}" \
                  --data-urlencode "text=❌ دانلود timeout | کاربر: ${{ github.event.inputs.user_id }}"
                exit 1
              fi
            elif [ $EXIT_CODE -ne 0 ]; then
              if [ $attempt -eq 1 ]; then
                curl -s -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage" \
                  -d "chat_id=${{ github.event.inputs.user_id }}" \
                  --data-urlencode "text=⚠️ دانلود با خطا مواجه شد. در حال تلاش مجدد..."
                sleep 5
                continue
              else
                curl -s -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage" \
                  -d "chat_id=${{ github.event.inputs.user_id }}" \
                  --data-urlencode "text=❌ دانلود ناموفق بود. لینک را بررسی کنید یا با ادمین تماس بگیرید: @MeliDownload_Admin"
                exit 1
              fi
            fi

            # پیدا کردن فایل دانلود شده
            DOWNLOADED_FILE=$(ls *.mp4 *.mp3 2>/dev/null | head -n1)

            if [ -z "$DOWNLOADED_FILE" ]; then
              echo "❌ فایل دانلود شده پیدا نشد"
              exit 1
            fi

            # بررسی محدودیت ۲۰۰ مگابایت
            FILE_SIZE=$(stat -c%s "$DOWNLOADED_FILE")
            if [ "$FILE_SIZE" -gt "$MAX_SIZE" ]; then
              curl -s -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage" \
                -d "chat_id=${{ github.event.inputs.user_id }}" \
                --data-urlencode "text=❌ حجم فایل بیش از ۲۰۰ مگابایت است و قابل پردازش نیست."
              rm -f "$DOWNLOADED_FILE"
              exit 1
            fi

            echo "DOWNLOADED_FILE=$DOWNLOADED_FILE" >> $GITHUB_ENV
            DOWNLOAD_SUCCESS=true
            echo "✅ دانلود با موفقیت انجام شد: $DOWNLOADED_FILE"
            break
          done

          if [ "$DOWNLOAD_SUCCESS" = false ]; then
            exit 1
          fi

      - name: ساخت آرشیو ۹۵ مگابایتی
        run: |
          7z a -mx=3 -v95m "archive.7z" "${{ env.DOWNLOADED_FILE }}"

      - name: حذف فایل اصلی
        run: rm -f "${{ env.DOWNLOADED_FILE }}"

      - name: تنظیم git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: شمارش تعداد پارت‌ها
        id: chunks
        run: |
          CHUNKS=$(ls archive.7z.* 2>/dev/null | wc -l)
          # اگه فقط یه پارت بود
          if [ "$CHUNKS" -eq 0 ] && [ -f "archive.7z" ]; then
            CHUNKS=1
          fi
          echo "total_chunks=$CHUNKS" >> $GITHUB_OUTPUT

      - name: اطلاع‌رسانی تعداد پارت‌ها
        run: |
          curl -s -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage" \
            -d "chat_id=${TELEGRAM_CHAT_ID}" \
            --data-urlencode "text=📦 تعداد پارت‌ها: ${{ steps.chunks.outputs.total_chunks }} | کاربر: ${{ github.event.inputs.user_id }}"

          curl -s -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage" \
            -d "chat_id=${{ github.event.inputs.user_id }}" \
            --data-urlencode "text=📦 فایل شما به ${{ steps.chunks.outputs.total_chunks }} پارت تقسیم شد. در حال آپلود..."

      - name: ساخت branch و push پارت‌ها
        run: |
          BRANCH_NAME="yt_${{ github.event.inputs.user_id }}_$(date +%s)"
          git checkout --orphan "$BRANCH_NAME"
          git rm -rf . 2>/dev/null || true
          echo "# YouTube download for user ${{ github.event.inputs.user_id }}" > README.md

          # هندل کردن یک یا چند پارت
          if [ -f "archive.7z" ]; then
            CHUNKS=(archive.7z)
          else
            CHUNKS=(archive.7z.*)
          fi

          total=${#CHUNKS[@]}
          batch_size=2
          start=0
          uploaded=0

          while [ $start -lt $total ]; do
            end=$((start + batch_size - 1))
            [ $end -ge $total ] && end=$((total - 1))

            for i in $(seq $start $end); do
              git add "${CHUNKS[$i]}"
            done
            git add README.md
            git commit -m "Add chunks $((start+1))-$((end+1)) of $total"

            pushed=false
            for attempt in {1..5}; do
              if git push origin "$BRANCH_NAME"; then
                uploaded=$((end+1))
                pushed=true
                break
              else
                echo "Push ناموفق (تلاش $attempt)، ۱۰ ثانیه صبر..."
                sleep 10
              fi
            done

            if [ "$pushed" = false ]; then
              echo "❌ Push پس از ۵ تلاش ناموفق بود"
              exit 1
            fi

            curl -s -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage" \
              -d "chat_id=${TELEGRAM_CHAT_ID}" \
              --data-urlencode "text=⏫ آپلود: ${uploaded}/${total} | کاربر: ${{ github.event.inputs.user_id }}"

            start=$((end + 1))
            sleep 2
          done

          echo "BRANCH_NAME=$BRANCH_NAME" >> $GITHUB_ENV

      - name: ارسال لینک دانلود به کاربر
        run: |
          DOWN="https://github.com/${{ github.repository }}/archive/refs/heads/${{ env.BRANCH_NAME }}.zip"

          curl -s -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage" \
            -d "chat_id=${TELEGRAM_CHAT_ID}" \
            --data-urlencode "text=🎉 کار تمام شد | کاربر: ${{ github.event.inputs.user_id }} | branch: ${{ env.BRANCH_NAME }}"

          MSG="${DOWN}
          
          لینک دانلود شما بعد از ۲ ساعت منقضی خواهد شد  👽
          
          📢 کانال ما: @MeliDownload_Bot"
          curl -s -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage" \
            -d "chat_id=${{ github.event.inputs.user_id }}" \
            --data-urlencode "text=${MSG}"

      - name: اطلاع‌رسانی در صورت شکست
        if: failure()
        run: |
          curl -s -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage" \
            -d "chat_id=${TELEGRAM_CHAT_ID}" \
            --data-urlencode "text=❌ Workflow یوتیوب شکست خورد | کاربر: ${{ github.event.inputs.user_id }}"

          curl -s -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage" \
            -d "chat_id=${{ github.event.inputs.user_id }}" \
            --data-urlencode "text=❌ متأسفانه پردازش فایل شما ناموفق بود. لطفاً با ادمین تماس بگیرید: @MeliDownload_Admin"
